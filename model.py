import torch.nn as nn

class Model(nn.Module):
    
    def __init__(self):
        super(Model, self).__init__()

        # generic
        self.flatten = nn.Flatten()
        self.relu = nn.

        # conv layers
        
        # linear layers
        nn.Linear()